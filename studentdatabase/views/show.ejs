   <html>
    <head>
        <title>Show details</title>
        <style>
         .head
            {
             Margin:0 auto;
             text-align:center;
            }
          .container{
                width:30%;
                text-align:center;
                border:1px solid grey;
                margin-left:500px;
                border-radius:10px;
                padding:20px 20px;
               }
            .l{
                font-size: 16px;
    	    	text-transform: uppercase;
    		  	padding: 10px 5px 10px 10px;
    			margin:20px 20px;
    			border-radius:5px;
               }
        </style>
    </head>
    <body>
         
     <div class="head">
       <h1>Student details </h1>
       <h3> Records of <%= table.name %> </h3>
   </div>
   <div class="container">
       <div>
        <labe class="l"> Name</label>
        <input class="l" type="text" value="<%= table.name %>">
        </div>
        <div >
        <label class="l"> Idno</label>
        <input  class="l" type="number" value="<%= table.idno %>">
        </div>
        <div>
        <label class="l"> Age</label>
        <input  class="l" type="text" value="<%= table.age %>">
        </div>
        <div >
        <label class="l">Gender</label>
        <input  class="l" type="text" value="<%= table.gender %>">
      </div>
        <form action="/stu/<%= table._id %>?_method=DELETE" method="POST">
                <button class="l">Delete</button>
            </form>
              <a href="/ "><button class="l">home</button></a>
   </div>
   
   </body>
   </html>
   
   
   
   
    